# Cloudflare only accepts 200, 301, 302, 303, 307 and 308 here. Making a path
# 404 is not done with a rule: the file simply does not exist in this build, and
# not_found_handling in wrangler.jsonc serves 404.html with a real 404 status.
# That already covers /wp-content/, /wp-includes/, /wp-admin/, /wp-json/ and
# xmlrpc.php, none of which exist here.
#
# The Rank Math export of 2026-09-08 shows an EMPTY redirections table, so there
# are no legacy 301s to carry over, and every old URL path is reproduced exactly.
# Any future forwarding rules go below, one per line: /old-path /new-path 301
